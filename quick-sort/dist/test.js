// Generated by CoffeeScript 2.0.0
(function() {
  var assert, quickSort;

  quickSort = require('./index');

  assert = require('assert');

  describe("quicksort algorithm", function() {
    it("checks base case", function() {
      var sortedArray, unsortedArray;
      unsortedArray = [10, 5, 2, 3];
      sortedArray = [2, 3, 5, 10];
      return assert.deepEqual(quickSort(unsortedArray), sortedArray);
    });
    it("checks first recursion case (0 elem in array)", function() {
      return assert.deepEqual(quickSort([]), []);
    });
    it("checks first recursion case (0 elem in array)", function() {
      return assert.deepEqual(quickSort([]), []);
    });
    it('check array with 1 elem', function() {
      return assert.deepEqual(quickSort([1]), [1]);
    });
    return it('checks second recursion case array with 2 sorted elems', function() {
      return assert.deepEqual(quickSort([1, 2]), [1, 2]);
    });
  });

}).call(this);

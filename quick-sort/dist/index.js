// Generated by CoffeeScript 2.0.0
(function() {
  var quickSort;

  quickSort = function(array) {
    var itemsAreLessPivotSubArray, itemsAreMoreThenPivotSubArray, pivot;
    if (array.length < 2) {
      return array;
    }
    if (array.length === 2) {
      // If first elem more than second will swap them and return it like new array.
      // or just return array sorted array with 2 items.
      if (array[0] < array[1]) {
        return array;
      } else {
        return [array[1], array[0]];
      }
    }
    pivot = array[0];
    itemsAreLessPivotSubArray = array.filter(function(item) {
      return item < pivot;
    });
    itemsAreMoreThenPivotSubArray = array.filter(function(item) {
      return item > pivot;
    });
    return [...quickSort(itemsAreLessPivotSubArray), pivot, ...quickSort(itemsAreMoreThenPivotSubArray)];
  };

  module.exports = quickSort;

}).call(this);
